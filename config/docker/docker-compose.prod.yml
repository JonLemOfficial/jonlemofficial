version: '3.8'

services:
  web:
    build:
      context: ../../
      dockerfile: config/docker/Dockerfile.prod
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
